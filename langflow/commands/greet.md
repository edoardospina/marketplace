---
description: Say Hello from Steve, with custom argument
argument-hint: [salutee]
---

# Output custom 'Hello' message

Output a simple 'Hello ' + $ARGUMENTS + ' from Steve!' and exit
If $ARGUMENTS is empty, just output 'Hello from Steve!' and exit
